
/**
 * Created by Sviridov on 5/1/2015.
 */
export declare class JsonSchemaGenerator {
    generateSchema(obj: any): Object;
    private pass(value, property);
    private passObject(obj, sch);
    private registerProperty(propName, value, sch);
    private passArray(array, property);
    private detectType(value);
}
export declare function generateSchema(text: string, mediaType: string): string;
