export declare class XMLValidator {
    private schema;
    private schemaObject;
    constructor(schema: string);
    validate(xml: string, references?: XMLSchemaReference[]): Error[];
    private remoteSchemas(refs);
}
export declare class XMLSchemaReference {
    originalPath: string;
    virtualIndex: number;
    patchedContent: string;
    constructor(originalPath: string, virtualIndex: number, patchedContent: string);
}
